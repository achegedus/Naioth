require 'spec_helper'

describe UserPreference do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    UserPreference.create!(@valid_attributes)
  end
end
